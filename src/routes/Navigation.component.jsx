import { Outlet } from "react-router-dom"

const Navigation = () => {
    return (
        <>
            <div>
                hi
            </div>
            <Outlet />
        </>
    )
}

export default Navigation